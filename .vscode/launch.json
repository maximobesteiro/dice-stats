{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Chrome",
            "type": "chrome",
            "request": "attach",
            "url": "http://localhost",
            "port": 3000,
            "webRoot": "${workspaceRoot}/src"
        }
    ]
}